Flerken Linux live-build config scripts

Requirements:
* Debian
* live-build (package)

Build command:
sudo lb clean
lb config
sudo lb build

Release: "Before of Pre-alpha"
Notes: Execute all with root permissions, you can use sudo
